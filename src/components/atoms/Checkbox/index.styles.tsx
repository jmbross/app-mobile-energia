import styled from 'styled-components';
import { Wrapper as OriginWrapper, Radio as OriginRadio, Text as OriginText } from '@/atoms/Radio/index.styles';

export const Wrapper = styled(OriginWrapper)``;

export const Checkbox = styled(OriginRadio)``;

export const Text = styled(OriginText)``;
