import styled from 'styled-components';
import { helpers } from '@/assets/styles';

export const UsageGroup = styled.div`
  ${helpers.flexSet('row', 'space-between', 'flex-end')};
  height: 85px;
`;

export const CustomLevel = styled.div`
  margin-right: 30px;
`;
